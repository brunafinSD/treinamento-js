console.log(typeof 'Bruna'); //operador unário que verifica o tipo de dado

const pessoa = {
    nome: 'Bruna',
    idade: 24
}

console.log(pessoa);

delete pessoa.idade; //operador unário que deleta o elemento

console.log(pessoa);